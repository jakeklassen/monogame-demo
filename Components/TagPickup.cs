namespace CherryBomb.Components
{
	// Marker for the one and only pickup: the cherry. Carries CollisionLayer Pickup
	// so the player (whose mask includes Pickup) collides with it.
	public struct TagPickup { }
}
