namespace CherryBomb.Components
{
	// Marks an entity as logically disabled (e.g. the boss during its death
	// sequence) so behaviour systems skip it while it is still rendered.
	// Source: tagDisabled.
	public struct TagDisabled { }
}
