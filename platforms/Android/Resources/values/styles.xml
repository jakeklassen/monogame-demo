<?xml version="1.0" encoding="utf-8"?>
<resources>
	<!--
		Fullscreen, no-action-bar theme used while the game initializes. Keeps a
		black background (matching the game's letterbox bars) instead of a flash of
		the system window background.
	-->
	<style name="Theme.Splash" parent="@android:style/Theme.NoTitleBar.Fullscreen">
		<item name="android:windowBackground">@android:color/black</item>
		<item name="android:windowNoTitle">true</item>
		<item name="android:windowFullscreen">true</item>
	</style>
</resources>
