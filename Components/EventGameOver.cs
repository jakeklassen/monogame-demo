namespace CherryBomb.Components
{
	// Emitted once when the player runs out of lives. No system consumes it yet —
	// the real GameOver screen transition lands in M5. For now its presence simply
	// marks that the game-over flow has fired.
	public class EventGameOver { }
}
